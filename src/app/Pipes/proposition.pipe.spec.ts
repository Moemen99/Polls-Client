import { PropositionPipe } from './proposition.pipe';

describe('PropositionPipe', () => {
  it('create an instance', () => {
    const pipe = new PropositionPipe();
    expect(pipe).toBeTruthy();
  });
});
